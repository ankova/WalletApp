'use strict';

describe('source module', function() {

  beforeEach(module('source'));

  describe('source controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('SourceCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});